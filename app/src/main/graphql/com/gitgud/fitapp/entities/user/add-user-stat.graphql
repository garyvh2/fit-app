# Write your query or mutation here
mutation addUserStat(
  $userId: ID!
  $userStat: HistoryStatsInputType!
) {
  addUserStat(
    userId: $userId
    userStat: $userStat
  ) {
   imc,
   height,
   weight
  }
}
